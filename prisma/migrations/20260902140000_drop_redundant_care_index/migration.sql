-- 같은 컬럼으로 유니크 인덱스와 일반 인덱스를 둘 다 만들어 두었다.
-- 조회는 유니크 인덱스가 받으므로 읽기는 그대로인데, 쓰기마다 인덱스를
-- 두 번 갱신한다. 매일 체크하는 표라 쓰기가 잦다.
--
-- 앞선 마이그레이션을 고치지 않고 새로 지운다. 이미 적용된 마이그레이션을
-- 수정하면 Prisma 가 체크섬 불일치로 배포를 막는다.
DROP INDEX IF EXISTS "child_care_log_lookup_idx";
