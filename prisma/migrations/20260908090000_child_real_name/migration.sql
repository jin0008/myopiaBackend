-- 자녀의 실제 이름.
--
-- 지금까지 nickname 한 칸만 있었는데, 화면에 부르는 말과 병원 기록에서
-- 찾을 이름은 다르다. 애칭은 그대로 두고 실명 칸을 따로 만든다.
--
-- 이미 등록된 아이는 실명을 받은 적이 없으므로 NULL 을 허용한다. 앱은
-- 새 등록에서만 필수로 받고, 기존 아이는 수정 화면에서 채우게 한다.
ALTER TABLE "parent_child_link" ADD COLUMN "name" TEXT;
